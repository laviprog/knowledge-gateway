info:
  name: Get providers
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_BASE_URL}}/providers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
