info:
  name: Get aggregated chat completion usage statistics
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_BASE_URL}}/chat-completion-requests/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
