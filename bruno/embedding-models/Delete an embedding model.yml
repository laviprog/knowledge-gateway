info:
  name: Delete an embedding model
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{API_BASE_URL}}/embedding-models/{{EMBEDDING_MODEL_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
