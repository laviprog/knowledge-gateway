info:
  name: Get embedding models
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_BASE_URL}}/embedding-models"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
