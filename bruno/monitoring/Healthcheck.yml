info:
  name: Healthcheck
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_BASE_URL}}/healthcheck"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
