info:
  name: Get LLM models
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_BASE_URL}}/llm-models"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
