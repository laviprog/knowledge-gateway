info:
  name: Delete a knowledge base
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{API_BASE_URL}}/knowledge-bases/{{KNOWLEDGE_BASE_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
