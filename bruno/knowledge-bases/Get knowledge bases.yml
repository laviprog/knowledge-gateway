info:
  name: Get knowledge bases
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_BASE_URL}}/knowledge-bases"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
