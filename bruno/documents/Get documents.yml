info:
  name: Get documents
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_BASE_URL}}/documents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
