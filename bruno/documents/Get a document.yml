info:
  name: Get a document
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_BASE_URL}}/documents/{{DOCUMENT_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
