info:
  name: Get API Keys
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_BASE_URL}}/users/{{USER_ID}}/api-keys"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
