info:
  name: Get a user by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_BASE_URL}}/users/{{USER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
