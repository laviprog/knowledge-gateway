info:
  name: Create an api-key
  type: http
  seq: 4

http:
  method: POST
  url: "{{API_BASE_URL}}/users/{{USER_ID}}/api-keys"
  body:
    type: json
    data: |-
      {
        "name": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
